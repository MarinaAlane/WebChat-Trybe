// tentar implementar isso no futuro -> deve ter outra forma de fazer isso melhor
// nao deu pra implementar, sem tempo pra "aturar" o lint...

// module.exports = (io) => {
//   io.on('connection', (socket) => {
//     console.log('user', socket.id, 'conectou');
//   });
// };
