const { ObjectId } = require('mongodb');
const connection = require('./connection');

const storeMessage = async (/** params */) => {};

const getMessages = async (/** params */) => {};

module.exports = {
  storeMessage,
  getMessages,
};
